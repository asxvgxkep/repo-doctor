[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "repo-doctor"
version = "0.1.0"
description = "A safe, local repository health checker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["typer>=0.12", "rich>=13.7"]

[project.scripts]
repo-doctor = "repo_doctor.cli:app"

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
norecursedirs = ["tests/fixtures"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
